# Server Port (default: 5000)
PORT=5000

# MongoDB Connection String (local MongoDB or MongoDB Atlas in production)
MONGO_URI=mongodb://127.0.0.1:27017/student_management_db

# JWT Secret Key for token signing and authentication
JWT_SECRET=college_sms_secret_key_2026

# Allowed Frontend URL for CORS (optional in production if frontend is served by Express; set if frontend is on separate domain)
# CLIENT_URL=https://your-frontend-domain.com
